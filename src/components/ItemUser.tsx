import Box from "@mui/material/Box";

// interface Props {
//   setShowAdding: React.Dispatch<React.SetStateAction<boolean>>;
// }
const ItemUser: React.FC /*<Props>*/ = (/*{ setShowAdding }*/) => {
  return (
    <Box sx={{ bgcolor: "gainsboro", width: 220, height: 150 }}>
      This is user list
    </Box>
  );
};
export default ItemUser;
